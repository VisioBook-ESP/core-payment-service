apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "core-payment-service.fullname" . }}-test-connection"
  labels:
    {{- include "core-payment-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "core-payment-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
